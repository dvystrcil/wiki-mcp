apiVersion: v1
kind: Service
metadata:
  name: wiki-mcp
  namespace: wiki-mcp
  labels:
    app.kubernetes.io/name: wiki-mcp
    app.kubernetes.io/part-of: homelab-mcp
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: wiki-mcp
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
